import { PerfumePriceFilterPipe } from './perfume-price-filter.pipe';

describe('PerfumePriceFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PerfumePriceFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
