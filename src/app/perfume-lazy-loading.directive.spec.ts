import { PerfumeLazyLoadingDirective } from './perfume-lazy-loading.directive';

describe('PerfumeLazyLoadingDirective', () => {
  it('should create an instance', () => {
    const directive = new PerfumeLazyLoadingDirective();
    expect(directive).toBeTruthy();
  });
});
